{
  "name": "mindserver-worker",
  "version": "1.0.0",
  "type": "module",
  "description": "Cloudflare Worker for MindServer LLM requests via OpenRouter",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
